{
  "name": "unsplash-downloader-with-api",
  "version": "0.0.1",
  "description": "Download from unsplash (uses the API so it doesn't break)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "async": "^2.0.1",
    "dotenv": "^2.0.0",
    "mkdirp": "^0.5.1",
    "request": "^2.74.0"
  },
  "jshintConfig": {
    "esversion": 6
  }
}
